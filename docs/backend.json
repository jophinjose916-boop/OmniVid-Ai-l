{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in the OmniVid AI application, linked to an external Google authentication system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "externalAuthId": {
          "type": "string",
          "description": "Identifier from the external authentication system (e.g., Google's user ID) used to link the app user to their external account. This is not a credential."
        },
        "email": {
          "type": "string",
          "description": "User's email address, obtained from the external authentication provider.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "User's display name, obtained from the external authentication provider."
        },
        "profileImageUrl": {
          "type": "string",
          "description": "URL to the user's profile image, obtained from the external authentication provider.",
          "format": "uri"
        },
        "preferredLanguage": {
          "type": "string",
          "description": "User's preferred language for the application interface and default prompt input (e.g., 'en', 'es', 'hi', 'de')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating the last time the user account details were updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "externalAuthId",
        "email",
        "createdAt",
        "updatedAt"
      ]
    },
    "Video": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Video",
      "type": "object",
      "description": "Represents a video generated by the OmniVid AI application from user prompts.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Video entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who generated this video. (Relationship: User 1:N Video)"
        },
        "originalPrompt": {
          "type": "string",
          "description": "The original text prompt provided by the user in their native language."
        },
        "optimizedPrompt": {
          "type": "string",
          "description": "The AI-optimized English version of the prompt used for video generation."
        },
        "targetVoiceoverLanguage": {
          "type": "string",
          "description": "The target language and dialect for the video's AI-generated voiceover (e.g., 'en-US', 'hi-IN', 'de-DE')."
        },
        "outputVideoUrl": {
          "type": "string",
          "description": "URL to the final generated video file, stored in Google Cloud Storage.",
          "format": "uri"
        },
        "thumbnailImageUrl": {
          "type": "string",
          "description": "URL to a thumbnail image for the generated video.",
          "format": "uri"
        },
        "status": {
          "type": "string",
          "description": "Current status of the video generation process (e.g., 'PENDING', 'GENERATING', 'COMPLETED', 'FAILED', 'SAFETY_BLOCKED')."
        },
        "resolution": {
          "type": "string",
          "description": "The resolution of the generated video (e.g., '720p', '1080p', '4k')."
        },
        "queueType": {
          "type": "string",
          "description": "The type of generation queue used for this video ('STANDARD' for longer wait, 'FAST_PASS' for ad-supported quicker generation)."
        },
        "durationSeconds": {
          "type": "number",
          "description": "The duration of the generated video in seconds."
        },
        "watermarked": {
          "type": "boolean",
          "description": "Indicates whether the generated video contains an invisible digital watermark."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the video generation request was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating the last time the video's status or details were updated.",
          "format": "date-time"
        },
        "generationStartedAt": {
          "type": "string",
          "description": "Timestamp indicating when the video generation process officially started.",
          "format": "date-time"
        },
        "generationCompletedAt": {
          "type": "string",
          "description": "Timestamp indicating when the video generation process was completed.",
          "format": "date-time"
        },
        "safetyScanStatus": {
          "type": "string",
          "description": "The result of the content safety scan for the generated video ('SCAN_PENDING', 'SAFE', 'FLAGGED', 'BLOCKED')."
        },
        "safetyScanDetails": {
          "type": "string",
          "description": "Optional details or reasons if the video was flagged or blocked by the safety scanner."
        }
      },
      "required": [
        "id",
        "userId",
        "originalPrompt",
        "optimizedPrompt",
        "targetVoiceoverLanguage",
        "status",
        "resolution",
        "queueType",
        "watermarked",
        "createdAt",
        "updatedAt",
        "safetyScanStatus"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles. The document ID matches the Firebase Auth UID.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/videos/{videoId}",
        "definition": {
          "entityName": "Video",
          "schema": {
            "$ref": "#/backend/entities/Video"
          },
          "description": "Stores videos generated by a specific user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            },
            {
              "name": "videoId",
              "description": "The unique identifier for a specific video."
            }
          ]
        }
      }
    ]
  }
}
